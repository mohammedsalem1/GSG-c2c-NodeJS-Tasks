# Task 9: Update ERD and Integrate Prisma with MySQL


## Description
Enhance the Task 7 Mini Project (TypeScript Express API) by:

1. Designing a clear **Entity Relationship Diagram (ERD)** to visualize data models and their relationships.
2. Replacing the **in-memory data storage** with a **MySQL database** using **Prisma ORM**.

## Git Workflow
- Create a dedicated branch: `task-9-update-ERD-with-prisma`
- Push all changes to this branch
- Create a **Pull Request (PR)** for review ⚠️ **Do NOT merge to `main`**
- Submit the branch URL as the assignment link
