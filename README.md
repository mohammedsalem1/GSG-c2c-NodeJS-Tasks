# Task 7 Mini Project Enhancement

Enhance the **Task 7 Mini Project (TypeScript Express API)** by completing the following steps:

---

## 1. Design an Entity Relationship Diagram (ERD)

- Create a clear ERD to **visualize data models** and their **relationships**.
- Include all entities, primary keys, foreign keys, and associations.

---

## 2. Replace In-Memory Data Storage

- Replace the current in-memory data storage with a **database**.
- Use **Prisma ORM** with **Mongoose** (MongoDB) to handle data persistence.
- Ensure all CRUD operations interact with the database correctly.

---

## 3. Follow a Professional Git Workflow

1. **Create a dedicated branch** from `main`:

```bash
git checkout -b task-10-update-ERD-with-prisma
